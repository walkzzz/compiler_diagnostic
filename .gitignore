# ===== Build artifacts =====
target/

# ===== Local SDK (compiler, ~1.2G) — provided by judge env via CANGJIE_HOME; NOT committed =====
cangjie-sdk-1.1.3/

# NOTE: cangjie-stdx-1.1.3.1/ is VENDORED and committed on purpose.
# cjpm.toml references it via a relative path so the repo is self-contained for
# stdx (no machine-specific / username-hardcoded absolute path). Only the
# compiler (cangjie-sdk-1.1.3) is expected from the judge environment.

# ===== Distribution zips =====
cangjie-sdk-windows-x64-1.1.3.zip
cangjie-stdx-windows-x64-1.1.3.1.zip

# ===== Expert-team bundle (not part of the library) =====
cangjie-expert-team-bundle/
cangjie-expert-team-bundle.zip

# ===== Temp / scratch =====
scratch/

# ===== Agent memory (WorkBuddy internal) =====
.workbuddy/

# ===== Leftover helper scripts / backups =====
add_imports.py
cjpm.toml.stdx.bak

# ===== Coverage / test runtime artifacts =====
*.gcda
*.gcno
cov_output/
cov_out/
build.log
cjlint.json
